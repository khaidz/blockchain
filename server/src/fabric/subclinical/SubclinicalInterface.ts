export interface Subclinical {
    subclinical_id: string;
    subclinical_name: string;
    // subclinical_price: string;
    subclinical_description?: string;
    subclinical_is_deleted: boolean;
    docType?: string; 
}